let firstName, lastName, fullName;
function getName(firstName, lastName, fullName) {
  fullName = `Welcome ${firstName} ${lastName} to our team .`;
  return fullName;
}

console.log(getName("Kareem", "Amara"));
